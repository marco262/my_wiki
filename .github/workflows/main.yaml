on:
  push

jobs:
  restart-server:
    steps:
      - name: Restart Server
        uses: fjogeleit/http-request-action@master
        with:
            url: 'http://mywiki.zapto.org:26262/load_changes'
            method: 'GET'